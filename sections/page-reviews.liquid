{%- comment -%}
  ============================================================
  REVIEWS PAGE SECTION — hero, stats, filter tabs, review cards grid, CTA.
  ============================================================
{%- endcomment -%}

<section class="km-reviews-page" {{ section.shopify_attributes }}>

  {%- comment -%} HERO STRIP {%- endcomment -%}
  <div class="km-reviews-page__hero">
    <div class="km-container km-reviews-page__hero-inner">
      {%- if section.settings.eyebrow != blank -%}
        <span class="km-reviews-page__eyebrow">{{ section.settings.eyebrow }}</span>
      {%- endif -%}
      <h1 class="km-reviews-page__h1">{{ section.settings.heading | default: 'Customer Reviews' }}</h1>
      {%- if section.settings.subheading != blank -%}
        <p class="km-reviews-page__sub">{{ section.settings.subheading }}</p>
      {%- endif -%}
      <div class="km-reviews-page__stats">
        <div class="km-reviews-page__stat">
          <div class="km-reviews-page__num-row">
            <span class="km-reviews-page__num">{{ section.settings.stat1_number | default: '4.9' }}</span>
            {%- if section.settings.stat1_show_stars -%}
              <span class="km-reviews-page__num-stars" aria-hidden="true">
                {%- for i in (1..5) -%}<span>{%- render 'icon', icon: 'star' -%}</span>{%- endfor -%}
              </span>
            {%- endif -%}
          </div>
          <div class="km-reviews-page__label">{{ section.settings.stat1_label | default: 'Average rating' }}</div>
        </div>
        <div class="km-reviews-page__stat">
          <div class="km-reviews-page__num-row"><span class="km-reviews-page__num">{{ section.settings.stat2_number | default: '12k+' }}</span></div>
          <div class="km-reviews-page__label">{{ section.settings.stat2_label | default: 'Bikes built' }}</div>
        </div>
        <div class="km-reviews-page__stat">
          <div class="km-reviews-page__num-row"><span class="km-reviews-page__num">{{ section.settings.stat3_number | default: '10 yrs' }}</span></div>
          <div class="km-reviews-page__label">{{ section.settings.stat3_label | default: 'Houston-built' }}</div>
        </div>
      </div>
    </div>
  </div>

  {%- comment -%} FUNCTIONAL FILTER TABS {%- endcomment -%}
  <div class="km-container km-reviews-page__tabs-wrap">
    <div class="km-reviews-page__tabs" role="tablist" data-km-rev-tabs>
      <button type="button" class="km-reviews-page__tab is-active" role="tab" aria-selected="true" data-filter="all">{{ section.settings.tab_all | default: 'All reviews' }}</button>
      {%- if section.settings.tab_1_label != blank -%}
        <button type="button" class="km-reviews-page__tab" role="tab" aria-selected="false" data-filter="{{ section.settings.tab_1_value | default: 'exhausts' }}">{{ section.settings.tab_1_label }}</button>
      {%- endif -%}
      {%- if section.settings.tab_2_label != blank -%}
        <button type="button" class="km-reviews-page__tab" role="tab" aria-selected="false" data-filter="{{ section.settings.tab_2_value | default: 'handlebars' }}">{{ section.settings.tab_2_label }}</button>
      {%- endif -%}
      {%- if section.settings.tab_3_label != blank -%}
        <button type="button" class="km-reviews-page__tab" role="tab" aria-selected="false" data-filter="{{ section.settings.tab_3_value | default: 'accessories' }}">{{ section.settings.tab_3_label }}</button>
      {%- endif -%}
      {%- if section.settings.tab_4_label != blank -%}
        <button type="button" class="km-reviews-page__tab" role="tab" aria-selected="false" data-filter="{{ section.settings.tab_4_value | default: 'service' }}">{{ section.settings.tab_4_label }}</button>
      {%- endif -%}
    </div>
    <div class="km-reviews-page__result-count" data-km-rev-count>Showing {{ section.blocks.size }} reviews</div>
  </div>

  {%- comment -%} REVIEW GRID — each card tagged with data-category for filter {%- endcomment -%}
  <div class="km-container km-reviews-page__grid-wrap">
    <div class="km-reviews-page__grid" data-km-rev-grid>
      {%- for block in section.blocks -%}
        {%- if block.type == 'review' -%}
          <article class="km-reviews-page__card" data-category="{{ block.settings.category }}" {{ block.shopify_attributes }}>
            {%- if block.settings.show_verified -%}
              <div class="km-reviews-page__verified">
                <span class="km-reviews-page__verified-dot"></span>
                {{ block.settings.verified_label | default: 'Verified Purchase' }}
              </div>
            {%- endif -%}
            <div class="km-reviews-page__stars" aria-label="{{ block.settings.rating | default: 5 }} out of 5 stars">
              {%- for i in (1..5) -%}<span>{%- render 'icon', icon: 'star' -%}</span>{%- endfor -%}
            </div>
            <p class="km-reviews-page__quote">{{ block.settings.quote }}</p>
            <div class="km-reviews-page__meta">
              <div class="km-reviews-page__author">{{ block.settings.author }}</div>
              <div class="km-reviews-page__product">{{ block.settings.product }}</div>
            </div>
          </article>
        {%- endif -%}
      {%- endfor -%}
    </div>

    <div class="km-reviews-page__empty" data-km-rev-empty hidden>
      {{ section.settings.empty_text | default: 'No reviews in this category yet.' }}
    </div>
  </div>

  {%- comment -%} CTA STRIP {%- endcomment -%}
  {%- if section.settings.show_cta -%}
    <div class="km-container">
      <div class="km-reviews-page__cta">
        <div>
          <h2>{{ section.settings.cta_heading | default: 'Bought something? Tell us how it went.' }}</h2>
          {%- if section.settings.cta_text != blank -%}
            <p>{{ section.settings.cta_text }}</p>
          {%- endif -%}
        </div>
        <a href="{{ section.settings.cta_link_url | default: '/pages/contact' }}" class="km-btn km-btn-primary">{{ section.settings.cta_link_label | default: 'Leave a review' }} {%- render 'icon', icon: 'arrow-right' -%}</a>
      </div>
    </div>
  {%- endif -%}
</section>

<style>
  .km-reviews-page { padding-bottom: 96px; background: var(--km-body-bg, #EDEDED); }
  .km-reviews-page__hero { background: #000; color: #fff; padding: 80px 0 64px; position: relative; overflow: hidden; isolation: isolate; }
  .km-reviews-page__hero::before { content: ''; position: absolute; inset: 0; z-index: -1; background: radial-gradient(60% 80% at 80% 30%, rgba(251,176,61,.15), transparent 60%); }
  .km-reviews-page__hero-inner { text-align: center; max-width: 760px; margin: 0 auto; }
  .km-reviews-page__eyebrow { display: inline-block; font-size: 12px; font-weight: 800; letter-spacing: .16em; text-transform: uppercase; color: var(--km-accent, #FBB03D); margin-bottom: 14px; }
  .km-reviews-page__h1 { font-size: clamp(40px, 5.4vw, 64px); font-weight: 700; letter-spacing: -.02em; color: #fff; margin: 0 0 16px; line-height: 1.05; }
  .km-reviews-page__sub { font-size: 16px; line-height: 1.65; color: rgba(255,255,255,.8); margin: 0 auto 40px; max-width: 560px; }
  .km-reviews-page__stats {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 40px;
    padding-top: 36px;
    border-top: 1px solid rgba(255,255,255,.15);
    max-width: 640px;
    margin: 0 auto;
  }
  .km-reviews-page__stat {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
  }
  .km-reviews-page__num-row {
    min-height: 60px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 6px;
  }
  .km-reviews-page__num {
    font-size: 36px;
    font-weight: 800;
    color: #fff;
    line-height: 1;
    letter-spacing: -.01em;
  }
  .km-reviews-page__num-stars {
    display: inline-flex;
    gap: 2px;
    color: var(--km-accent, #FBB03D);
  }
  .km-reviews-page__num-stars svg { width: 12px; height: 12px; }
  .km-reviews-page__label {
    font-size: 11px;
    font-weight: 700;
    letter-spacing: .12em;
    text-transform: uppercase;
    color: rgba(255,255,255,.6);
    margin-top: 10px;
  }

  .km-reviews-page__tabs-wrap { display: flex; flex-direction: column; align-items: center; gap: 14px; margin: 48px auto 28px; }
  .km-reviews-page__tabs {
    display: flex;
    gap: 4px;
    padding: 8px;
    background: #fff;
    border: 1px solid var(--km-border, #DDDDDD);
    border-radius: 9999px;
  }
  .km-reviews-page__tab {
    padding: 12px 24px;
    font-size: 12px;
    font-weight: 800;
    letter-spacing: .1em;
    text-transform: uppercase;
    color: var(--km-text, #4B5563);
    background: transparent;
    border: 0;
    border-radius: 9999px;
    cursor: pointer;
    font-family: inherit;
    transition: background .25s ease, color .25s ease;
    white-space: nowrap;
  }
  .km-reviews-page__tab:hover { color: #000; background: var(--km-body-bg, #EDEDED); }
  .km-reviews-page__tab.is-active { background: #000; color: #fff; }
  .km-reviews-page__result-count { font-size: 12px; color: var(--km-text, #4B5563); font-weight: 600; letter-spacing: .02em; }

  .km-reviews-page__grid-wrap { margin-bottom: 64px; }
  .km-reviews-page__grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; }
  .km-reviews-page__card {
    background: #fff;
    border: 1px solid var(--km-border, #DDDDDD);
    border-radius: 20px;
    padding: 28px;
    display: flex;
    flex-direction: column;
    gap: 14px;
    transition: transform .25s ease, box-shadow .25s ease, opacity .3s ease, scale .3s ease;
    transform-origin: center;
  }
  .km-reviews-page__card:hover { transform: translateY(-3px); box-shadow: 0 8px 22px rgba(0,0,0,.06); }
  .km-reviews-page__card[hidden] { display: none; }
  .km-reviews-page__card.is-entering { animation: km-rev-fade .35s ease both; }
  @keyframes km-rev-fade {
    from { opacity: 0; transform: translateY(8px) scale(.98); }
    to   { opacity: 1; transform: translateY(0) scale(1); }
  }

  .km-reviews-page__verified { display: inline-flex; align-items: center; gap: 8px; font-size: 11px; font-weight: 700; letter-spacing: .1em; text-transform: uppercase; color: #0a8453; background: rgba(10,132,83,.08); padding: 6px 12px; border-radius: 9999px; align-self: flex-start; }
  .km-reviews-page__verified-dot { width: 6px; height: 6px; border-radius: 50%; background: #0a8453; }
  .km-reviews-page__stars { display: inline-flex; gap: 2px; color: var(--km-accent, #FBB03D); }
  .km-reviews-page__quote { font-size: 15px; line-height: 1.65; color: var(--km-text, #4B5563); margin: 0; font-weight: 500; flex: 1; }
  .km-reviews-page__meta { padding-top: 14px; border-top: 1px solid var(--km-border, #DDDDDD); }
  .km-reviews-page__author { font-weight: 700; font-size: 14px; color: #000; }
  .km-reviews-page__product { font-size: 12px; color: var(--km-text, #4B5563); margin-top: 4px; line-height: 1.4; }

  .km-reviews-page__empty {
    text-align: center;
    padding: 60px 20px;
    background: #fff;
    border: 1px dashed var(--km-border, #DDDDDD);
    border-radius: 20px;
    color: var(--km-text, #4B5563);
    font-size: 15px;
  }

  .km-reviews-page__cta { background: #fff; border: 1px solid var(--km-border, #DDDDDD); border-radius: 24px; padding: 40px 48px; display: flex; justify-content: space-between; align-items: center; gap: 32px; }
  .km-reviews-page__cta h2 { font-size: 24px; font-weight: 700; color: #000; margin: 0 0 6px; letter-spacing: -.01em; }
  .km-reviews-page__cta p { font-size: 14px; color: var(--km-text, #4B5563); margin: 0; max-width: 520px; }
  .km-btn { display: inline-flex; align-items: center; gap: 8px; padding: 14px 28px; border-radius: 9999px; text-decoration: none; font-weight: 700; font-size: 14px; white-space: nowrap; }
  .km-btn-primary { background: #000; color: #fff; }
  .km-btn-primary:hover { background: var(--km-accent, #FBB03D); color: #000; }

  @media (max-width: 900px) { .km-reviews-page__grid { grid-template-columns: 1fr; } .km-reviews-page__cta { flex-direction: column; text-align: center; padding: 32px 24px; } }
  @media (max-width: 700px) {
    .km-reviews-page__tabs { flex-wrap: wrap; justify-content: center; gap: 6px; border-radius: 20px; }
    .km-reviews-page__tab { padding: 10px 16px; font-size: 11px; }
  }
  @media (max-width: 600px) { .km-reviews-page__stats { gap: 24px; } .km-reviews-page__num { font-size: 24px; } }
</style>

<script>
(() => {
  const tabsRoot = document.querySelector('[data-km-rev-tabs]');
  const grid = document.querySelector('[data-km-rev-grid]');
  const count = document.querySelector('[data-km-rev-count]');
  const empty = document.querySelector('[data-km-rev-empty]');
  if (!tabsRoot || !grid) return;

  const tabs = [...tabsRoot.querySelectorAll('[data-filter]')];
  const cards = [...grid.querySelectorAll('[data-category]')];

  const applyFilter = (filter) => {
    let visible = 0;
    cards.forEach((card) => {
      const matches = filter === 'all' || card.getAttribute('data-category') === filter;
      if (matches) {
        card.hidden = false;
        card.classList.remove('is-entering');
        // trigger reflow so animation replays
        void card.offsetWidth;
        card.classList.add('is-entering');
        visible++;
      } else {
        card.hidden = true;
        card.classList.remove('is-entering');
      }
    });
    if (count) count.textContent = visible === 1 ? 'Showing 1 review' : 'Showing ' + visible + ' reviews';
    if (empty) empty.hidden = visible > 0;
  };

  tabs.forEach((tab) => {
    tab.addEventListener('click', () => {
      const filter = tab.getAttribute('data-filter') || 'all';
      tabs.forEach((t) => {
        t.classList.toggle('is-active', t === tab);
        t.setAttribute('aria-selected', t === tab ? 'true' : 'false');
      });
      applyFilter(filter);
      // Scroll the grid into view on mobile so the user sees the filtered results
      if (window.innerWidth < 900) {
        grid.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }
    });
  });

  // Initialise count on first load
  applyFilter('all');
})();
</script>

{% schema %}
{
  "name": "Reviews page",
  "tag": "section",
  "class": "shopify-section-reviews-page",
  "settings": [
    { "type": "header", "content": "Hero" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Real Rider Feedback" },
    { "type": "text", "id": "heading", "label": "H1 heading", "default": "Customer Reviews" },
    { "type": "textarea", "id": "subheading", "label": "Subheading", "default": "The stories behind 12,000+ builds. Every review below is from a verified customer who purchased and installed our parts." },

    { "type": "header", "content": "Stats row" },
    { "type": "text", "id": "stat1_number", "label": "Stat 1 number", "default": "4.9" },
    { "type": "text", "id": "stat1_label", "label": "Stat 1 label", "default": "Average rating" },
    { "type": "checkbox", "id": "stat1_show_stars", "label": "Show star row under Stat 1", "default": true },
    { "type": "text", "id": "stat2_number", "label": "Stat 2 number", "default": "12k+" },
    { "type": "text", "id": "stat2_label", "label": "Stat 2 label", "default": "Bikes built" },
    { "type": "text", "id": "stat3_number", "label": "Stat 3 number", "default": "10 yrs" },
    { "type": "text", "id": "stat3_label", "label": "Stat 3 label", "default": "Houston-built" },

    { "type": "header", "content": "Filter tabs" },
    { "type": "paragraph", "content": "The first tab is always 'All'. Set other tab labels and the corresponding category slug (must match the review block's 'category' value)." },
    { "type": "text", "id": "tab_all", "label": "All-tab label", "default": "All reviews" },
    { "type": "text", "id": "tab_1_label", "label": "Tab 1 label", "default": "Exhausts" },
    { "type": "text", "id": "tab_1_value", "label": "Tab 1 filter value", "default": "exhausts" },
    { "type": "text", "id": "tab_2_label", "label": "Tab 2 label", "default": "Handlebars" },
    { "type": "text", "id": "tab_2_value", "label": "Tab 2 filter value", "default": "handlebars" },
    { "type": "text", "id": "tab_3_label", "label": "Tab 3 label", "default": "Accessories" },
    { "type": "text", "id": "tab_3_value", "label": "Tab 3 filter value", "default": "accessories" },
    { "type": "text", "id": "tab_4_label", "label": "Tab 4 label", "default": "Service" },
    { "type": "text", "id": "tab_4_value", "label": "Tab 4 filter value", "default": "service" },

    { "type": "header", "content": "Empty state" },
    { "type": "text", "id": "empty_text", "label": "Empty-state text", "default": "No reviews in this category yet." },

    { "type": "header", "content": "Bottom CTA" },
    { "type": "checkbox", "id": "show_cta", "label": "Show bottom CTA", "default": true },
    { "type": "text", "id": "cta_heading", "label": "CTA heading", "default": "Bought something? Tell us how it went." },
    { "type": "textarea", "id": "cta_text", "label": "CTA text", "default": "Your review helps other riders pick the right parts — and helps us keep making better ones." },
    { "type": "text", "id": "cta_link_label", "label": "CTA link label", "default": "Leave a review" },
    { "type": "url", "id": "cta_link_url", "label": "CTA link URL" }
  ],
  "blocks": [
    {
      "type": "review",
      "name": "Review",
      "settings": [
        { "type": "textarea", "id": "quote", "label": "Quote", "default": "Great quality parts." },
        { "type": "text", "id": "author", "label": "Author", "default": "Verified Buyer" },
        { "type": "text", "id": "product", "label": "Product / subtitle", "default": "Product name" },
        { "type": "range", "id": "rating", "label": "Rating (visual)", "min": 1, "max": 5, "step": 1, "default": 5 },
        { "type": "text", "id": "category", "label": "Category (must match filter value)", "default": "accessories" },
        { "type": "checkbox", "id": "show_verified", "label": "Show 'Verified Purchase' badge", "default": true },
        { "type": "text", "id": "verified_label", "label": "Verified badge text", "default": "Verified Purchase" }
      ]
    }
  ],
  "max_blocks": 50,
  "presets": [
    {
      "name": "Reviews page",
      "category": "Pages",
      "blocks": [
        { "type": "review", "settings": { "quote": "Great quality parts. Fast shipping.", "author": "Chad Ramsey", "product": "Complete Tank Lift & Coil + Key Relocation Kit", "rating": 5, "category": "accessories" } },
        { "type": "review", "settings": { "quote": "They're quick and effective. Products perfect as described. Recommended to everyone. They're also quick on call-backs when you leave a message.", "author": "Verified Buyer", "product": "Slip-On Exhaust — Harley Sportster", "rating": 5, "category": "exhausts" } },
        { "type": "review", "settings": { "quote": "Little hiccup in the delivery, but as usual Kinetic made haste to correct the missing brake line from my order.", "author": "Christopher Cook", "product": "Composite Wide Grip for Harley Sportster", "rating": 5, "category": "accessories" } },
        { "type": "review", "settings": { "quote": "Very high quality, excellent fit. Look great.", "author": "Steven Harrold", "product": "Heavy Knurl Shifter Peg for Sportster", "rating": 5, "category": "accessories" } },
        { "type": "review", "settings": { "quote": "Bolt-on fit was flawless. Throttle response is night-and-day better. Would buy again.", "author": "Jordan M.", "product": "Slip-On Exhaust for Harley Sportster", "rating": 5, "category": "exhausts" } },
        { "type": "review", "settings": { "quote": "Shipped fast and packed well. The finish matches the photos exactly.", "author": "Priya S.", "product": "Moto-style Handlebars for Triumph Street Twin", "rating": 5, "category": "handlebars" } },
        { "type": "review", "settings": { "quote": "Great kit, simple install. Took me about 2 hours start to finish.", "author": "Diego L.", "product": "Tank Lift & Coil Relocation Kit", "rating": 5, "category": "accessories" } },
        { "type": "review", "settings": { "quote": "Customer service was on point — answered every question I had pre-purchase.", "author": "Sarah K.", "product": "MiniZ Handlebars — Aggressive Sportster", "rating": 5, "category": "handlebars" } },
        { "type": "review", "settings": { "quote": "Outstanding craftsmanship. The CNC work on these footpegs is unreal.", "author": "Tom R.", "product": "Kinetic CNC Footpegs — Sportster", "rating": 5, "category": "accessories" } },
        { "type": "review", "settings": { "quote": "Response time from support was under 2 hours. Helped me pick the right pipes for my '18 Dyna.", "author": "Marcus T.", "product": "Consultation & Fitment Check", "rating": 5, "category": "service" } },
        { "type": "review", "settings": { "quote": "Deep, throaty tone exactly like the video. Installation took 45 minutes.", "author": "Eli R.", "product": "2-into-1 Exhaust — Indian Scout", "rating": 5, "category": "exhausts" } },
        { "type": "review", "settings": { "quote": "Went back and forth on phone before ordering — the team was patient and knowledgeable.", "author": "Kendall P.", "product": "Pre-sale support", "rating": 5, "category": "service" } }
      ]
    }
  ]
}
{% endschema %}
